This directory contains older application code kept for reference.

`legacy/scripts/` is a previous version of the project and is not the active implementation.

The current application lives in `app/` and `src/`.
